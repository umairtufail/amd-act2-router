# Core runtime dependencies (kept minimal so the container stays slim and boots fast).
torch>=2.0.0
transformers>=4.30.0
pydantic>=2.0.0
requests>=2.28.0
python-dotenv>=1.0.0
pyyaml>=6.0
numpy>=1.24.0
streamlit==1.59.1
pandas==3.0.3